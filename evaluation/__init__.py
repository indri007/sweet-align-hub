"""
RAG evaluation tooling for the job-matching pipeline (agents/rag_agent.py),
using Ragas (https://docs.ragas.io/).

This is a developer/CI tool, not an end-user app feature — the project
requirements don't ask for it to be exposed in the Streamlit UI. Run it with:

    python evaluation/run_ragas_eval.py

See evaluation/README.md for details.
"""
